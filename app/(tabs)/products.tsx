import React from "react";
import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const ProductsList = () => {
  return (
    <SafeAreaView>
      <Text>List of products List</Text>
    </SafeAreaView>
  );
};

export default ProductsList;
